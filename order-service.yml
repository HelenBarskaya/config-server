spring:

  datasource:
    url: jdbc:postgresql://localhost:5416/orders
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop